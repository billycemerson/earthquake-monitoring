name: 'bmkg_pipeline'
version: '1.0.0'
profile: 'bmkg_pipeline'

model-paths: ["models"]
test-paths:  ["tests"]
macro-paths: ["macros"]
seed-paths:  ["seeds"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  bmkg_pipeline:
    example:
      +materialized: view
